enum RoomType { standard, deluxe, suite }

enum RequestStatus { pending, active}

enum BookingStatus { active, cancelled, completed }

enum ServiceType { meal, laundry, spa, housekeeping, otherservices }
